name: tgbot_voxcpm

services:
  bot:
    image: ${DOCKER_IMAGE:-tgbot_voxcpm:local}
    build:
      context: .
      args:
        VERSION: ${VERSION:-0.0.0}
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
